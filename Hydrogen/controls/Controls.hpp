#pragma once
#include <string>

namespace ctrl {
	/// Example keybind, used to open the menu
	extern const std::string KeyOpenMenu;
	/// Example keybind, used to open the game
	extern const std::string KeyOpenGame;
}